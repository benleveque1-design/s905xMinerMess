# Shared secret used by the controller, all worker agents, and mutating REST calls.
# Generate one with: openssl rand -hex 32
WORKER_AUTH_TOKEN=change-me

# Optional overrides (defaults shown)
PORT=3010
NODE_ENV=production
